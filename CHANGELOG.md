# Changelog

## 0.6.0 — Talent Identity & Interview Collaboration

- 候选人新建与简历报告保存统一按标准化手机号、邮箱查重，任一命中即阻止重复建档；
- 重复命中时展示已有档案、命中字段，并支持把新资料和匹配报告合并到已有候选人；
- 候选人详情自动识别历史重复档案，确认后合并招聘流程、匹配报告、面试记录和原始简历；
- 新增面试排期模型，维护时间、时长、面试官、地点、会议链接和候选人说明；
- 保存排期后自动把关联招聘流程推进到面试阶段，招聘概览读取实际未来排期；
- 支持调用本机默认邮箱生成面试邀请，并导出标准 `.ics` 文件到 Outlook、Apple Calendar 等日历；
- 面试中心新增近期排期总览与邮件、日历快捷操作；
- 新增 DeepSeek 专属模型配置模板。

## 0.5.0 — Lightweight ATS Edition

- GitHub发布版改为空白数据库，不再自动写入演示职位、候选人或漏斗数据；
- 招聘概览、职位人数和招聘漏斗统一由实际 `Application` 流程记录计算；
- 全站业务术语统一为“职位”；
- 职位管理和候选人管理均提供“添加候选人”入口，可加入指定职位流程或仅加入人才库；
- 候选人列表新增按招聘职位筛选；候选人详情支持维护各职位的招聘阶段；
- 支持保存并下载候选人原始简历，电话和邮箱默认遮罩、按需查看；
- 联系方式查看、简历下载和阶段更新写入审计日志；
- 启动器自动同步数据库结构，并将旧版候选人与职位关系迁移为招聘流程记录。

## 0.4.2 — Candidate Identity Controls

- 简历解析默认保留候选人姓名，便于候选人列表、报告与后续流程管理；
- 新增“保留姓名 / 匿名化”解析选项，只有主动选择匿名化时才使用“候选人 A”；
- 匿名化策略从强制规则调整为HR可控配置，同时继续强制脱敏手机号、邮箱和身份证号；
- 原始上传文件与简历正文仍不持久化，保存报告时仅保存结构化字段和匹配证据；
- 完成DeepSeek真实链路下的保留姓名与匿名化双路径回归验证。
- 修复数据库文件已存在但Prisma Client尚未生成时，职位与候选人接口返回500的问题；
- 职位和候选人页面新增读取失败提示与重试入口，不再无限显示加载状态。

## 0.4.1 — Resume Isolation Fix

- 修复Mock模式上传任意简历都会复用销售候选人Demo资料的问题；
- Mock简历解析改为仅从本次上传文本提取，缺失字段显示“待确认”；
- Mock人职匹配改为按本次职位和候选人进行基础规则评分，不再复用Demo分数；
- 简历页明确展示本次解析Provider和模型，Mock模式显示醒目提示；
- 模型设置页在Mock模式下不再错误展示真实模型名称；
- 新增匿名HRBP回归简历，用于验证不同候选人之间的数据隔离。

## 0.4.0 — Showcase & Deployment Edition

- 新增Windows双击一键启动器，自动准备依赖、数据库、服务和浏览器；
- 新增产品案例、3–5分钟面试讲稿与常见追问回答；
- 新增Docker/云平台部署指南和GitHub发布检查清单；
- 健康检查支持在启用访问码后被容器平台访问，并减少配置暴露；
- Docker镜像与Compose新增健康检查和环境变量注入；
- 修复浏览器翻译扩展注入属性导致的开发环境hydration警告；
- 新增公开仓库安全策略和真实招聘数据提交警告。

## 0.3.0 — DeepSeek & Trust Edition

- 完成 DeepSeek OpenAI-compatible 接入与真实端到端验证；
- 增强结构化输出兼容层，处理常见JSON包装和字段类型偏差；
- 新增确定性的匹配等级规则，仅80分及以上提示进入面试；
- 招聘漏斗升级为实时规则诊断 + AI原因假设与行动建议；
- 新增漏斗阶段人数顺序校验，避免出现超过100%的无效转化；
- 简历正文、候选人与匹配结果不再写入浏览器localStorage；
- 保存候选人报告时仅持久化匿名结构化信息，不保存简历正文；
- 补充虚构JD、匿名简历、README截图及DeepSeek配置说明。

## 0.2.0 — Portfolio Edition

- 新增职位管理与招聘状态维护；
- 新增候选人管理、详情报告、HR流程结论和备注；
- 新增OpenAI Responses、OpenAI-compatible和Anthropic Provider；
- 新增模型状态页和服务端连接测试；
- 新增匹配报告与面试指南打印/PDF版式；
- 新增可选访问码登录、HttpOnly Cookie和退出登录；
- 新增Dockerfile、Docker Compose和健康检查接口；
- Demo种子数据升级为职位、候选人、匹配结果和招聘漏斗的完整链路。

## 0.1.0 — MVP

- JD智能分析；
- PDF、DOCX、TXT简历解析和基础脱敏；
- 人职匹配与可追溯证据；
- STAR面试问题与招聘漏斗Dashboard。
