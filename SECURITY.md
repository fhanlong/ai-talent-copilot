# Security Policy

AI Talent Copilot 是招聘辅助工具。请勿在公开Demo、Issue、Pull Request或截图中提交真实候选人的姓名、联系方式、身份证号、完整简历或未经授权的招聘数据。

发现安全问题时，请使用GitHub仓库的私有 Security Advisory 报告，不要创建包含漏洞细节、密钥或个人数据的公开Issue。

如果API Key、访问码或认证密钥曾出现在提交、日志或截图中，应立即在对应服务控制台撤销并重新生成；仅从Git历史中删除字符串并不能使已泄露的密钥重新安全。

当前SQLite版本面向本地MVP和单实例作品集演示，不应直接作为存储真实招聘数据的企业生产系统。

简历解析默认保留候选人姓名，以支持HR的后续管理；用户可在解析前切换为匿名化。无论是否匿名，手机号、邮箱和身份证号都会在模型调用前进行基础脱敏。解析请求本身不会自动保存文件；HR主动保存候选人后，原始简历、电话和邮箱会写入本地SQLite，简历正文不单独持久化。候选人详情默认遮罩联系方式，查看联系方式和下载简历会写入审计日志。仅应在获得相应授权、启用访问码并具备适当设备与备份保护的环境中处理真实候选人。
